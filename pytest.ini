[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -ra --strict-markers
markers =
    criteria: maps to one of the six stated success criteria
    slow: takes more than a few seconds
filterwarnings =
    ignore::DeprecationWarning
